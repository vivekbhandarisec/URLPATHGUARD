ShieldPath test data files

Files created:
- /mnt/data/shieldpath_test/access.log
- /mnt/data/shieldpath_test/ipdr.csv
- /mnt/data/shieldpath_test/traffic.pcap

PCAP placeholder created (scapy not available). You may replace traffic.pcap with a real pcap if needed.

Usage:
Use the producers in your project to read these files and send to Kafka.
